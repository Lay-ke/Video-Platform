const { Router } = require('express');

const router = Router();

router.get('/signin', () => {});
router.post('/signin', () => {});
router.get('/signup', () => {});
router.post('/signup', () => {});
